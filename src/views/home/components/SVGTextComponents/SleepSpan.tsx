import classes from "../circle.module.scss";
export const Sleep: React.FC = () => {
  return (
    <tspan className={classes.circleText}>
      else {`{Conall.sleep();}`} else {`{Conall.sleep()}`} else{" "}
      {`{Conall.sleep()}`}
    </tspan>
  );
};
