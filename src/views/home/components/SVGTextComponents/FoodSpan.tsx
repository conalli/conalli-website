import classes from "../circle.module.scss";
export const Food: React.FC = () => {
  return (
    <tspan className={classes.circleText}>
      if (hungry) {`{Conall.eat();}`} if (hungry) {`{Conall.eat();}`}
    </tspan>
  );
};
